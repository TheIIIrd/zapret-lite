* text=auto eol=lf

# Git не должен трогать содержимое vendor: там двоичные пейлоады .bin,
# а списки уже приведены к LF инструментом синхронизации. Нормализация
# на стороне git сломала бы .bin и разошлась бы с MANIFEST.sha256.
vendor/** -text
